# PyHiveLMS is published to a small PEP 503 index in System-B90/.github, not to
# PyPI, which stops at 2.1.1. The index is served by GitHub Pages — NOT by
# raw.githubusercontent.com, which cannot back a pip index at all: it 404s the
# directory URL pip requests, and serves index.html as text/plain with nosniff
# so --find-links will not scrape it either.
--extra-index-url https://system-b90.github.io/.github/pypi/

InquirerPy>=0.3.4
prompt-toolkit>=3.0.53
python-dotenv==1.2.2
typer==0.27.0
PyHiveLMS>=2.1.3
# Shared dev-ops helpers, published to the same org index. See
# System-B90/Bluz#226 and System-B90/devops-py.
sb90-devops>=0.1.0
# Shared install/upgrade/bundle flows (System-B90/deploy-py). scripts/setup.py
# and scripts/tests import it; release bundles vendor it themselves.
sb90-deploy>=0.1.0
tqdm>=4.70.0
# The bluz CLI's own runtime deps, so e2e can drive `bluz auth login` (#580).
httpx>=0.28.0
rich>=13.0.0
ruff==0.16.8
pytest>=8.0.0
