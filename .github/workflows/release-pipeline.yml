name: Build and Release Bluz

on:
    # push is restricted to the long-lived branches on purpose. With
    # `push: ["**"]` alongside `pull_request: ["**"]`, every commit on a PR
    # branch ran this workflow twice — once per event — and the two runs did
    # not cancel each other, because the concurrency key includes github.ref,
    # which is refs/heads/<branch> for push but refs/pull/N/merge for
    # pull_request. That doubled every PR's CI cost on a shared runner.
    push:
        branches: [master, dev]
        tags: ["v*.*.*", "v*.*.*-rc*", "ci-test"]
    pull_request:
        branches: ["**"]

# Cancel in-progress runs for PRs to save runner minutes
concurrency:
    group: ${{ github.workflow }}-${{ github.ref }}
    cancel-in-progress: true

env:
    REGISTRY: ghcr.io
    IMAGE_NAME_UI: system-b90/bluz/ui
    IMAGE_NAME_SESSIONS: system-b90/bluz/sessions
    IMAGE_NAME_PROXY: system-b90/bluz/proxy

jobs:
    # Lint and unit tests were separate jobs, each with its own checkout, Node
    # setup and `npm ci` — a second full install for no parallelism worth
    # paying for. `!cancelled()` on the later steps preserves the one thing the
    # split did provide: a lint failure still doesn't hide a test failure.
    verify:
        name: Lint + Typecheck + Unit Tests
        # GitHub-hosted while the org's self-hosted runner is offline (mirrors
        # pyhive's publish.yml). Nothing here boots Hive or needs the
        # self-hosted box — plain Node/Python with standard actions.
        runs-on: ubuntu-latest
        timeout-minutes: 60
        env:
            # Same reason as ui/Dockerfile's builder stage: V8 derives its
            # default heap from host RAM, so `eslint ui/` got ~4G on the 23G
            # self-hosted box and ~2G here, where it dies with "Ineffective
            # mark-compacts near heap limit". Job-level so the typecheck and
            # unit-test steps get the same ceiling.
            NODE_OPTIONS: --max-old-space-size=6144
        # Dependabot PRs are single dep bumps; CI on them wastes runner
        # minutes. Run this only for humans. Version-bump commits on master
        # (chore: bump version to *) are metadata-only, so skip CI on them too.
        #
        # The bump-commit skip is scoped to branch pushes. publish.py tags the
        # very commit it bumps, so the release tag arrives carrying the same
        # head_commit.message — and an unscoped check skipped the entire release
        # pipeline, taking build-ui/sessions/proxy and craft-release down with
        # it. v1.0.0-rc.2 was tagged into that hole and published nothing.
        if: >-
            github.actor != 'dependabot[bot]'
            && !(github.ref_type == 'branch'
                 && startsWith(github.event.head_commit.message, 'chore: bump version to'))
        steps:
            - name: Checkout repository
              uses: actions/checkout@v7

            # `cache:` was off while this job ran on the self-hosted box: that
            # runner had persistent disk, so the npm cache already survived
            # between runs, while the actions/cache blob grew to 26GB and
            # uploading it at self-hosted bandwidth overran the job timeout.
            # GitHub-hosted runners are ephemeral — nothing survives — so the
            # cache is now the only thing keeping `npm ci` off a cold install.
            - name: Setup Node.js 24
              uses: actions/setup-node@v7
              with:
                  node-version: "24"
                  cache: npm
                  cache-dependency-path: |
                      package-lock.json
                      session-server/package-lock.json

            - name: Setup Python
              uses: actions/setup-python@v7
              with:
                  python-version: "3.11"

            - name: Install Dependencies (Node)
              run: npm ci
              env:
                  NPM_TOKEN: ${{ secrets.CLASSIC_ACCESS_TOKEN || secrets.HIVE_REPO_TOKEN }}

            - name: Install Dependencies (Python)
              # scripts/requirements.txt covers scripts/tests (it pulls
              # sb90-devops from the org pip index, which tools_impl.py
              # imports); ./cli covers cli/tests. Ruff is pinned exactly in
              # scripts/requirements.txt — an upstream release that promotes a
              # rule must not turn this job red on an unrelated PR.
              run: |
                  pip install -r scripts/requirements.txt
                  pip install ./cli

            - name: Run ESLint
              run: npm run lint

            - name: Run Ruff (Python Linter)
              if: ${{ !cancelled() }}
              run: ruff check .

            - name: Run Ruff (Python Formatter Check)
              if: ${{ !cancelled() }}
              run: ruff format --check .

            # The only tsc invocation in this pipeline used to be inside
            # `next build`, which is skipped on pull requests — so a PR with a
            # type error passed every check and broke master. `tsc --noEmit` is
            # far cheaper than a build and covers the gap. See #344.
            - name: Type-check UI
              if: ${{ !cancelled() }}
              run: npm run typecheck

            - name: Run Unit Tests
              if: ${{ !cancelled() }}
              run: npm run test:unit
              env:
                  NEXTAUTH_SECRET: ci_test_secret_key

            # Nothing ran the Python side before: cli/ had no tests at all, and
            # the loopback login handshake is exactly the kind of thing that
            # breaks silently — it regressed to sitting out its full 60s
            # timeout with no failing check anywhere.
            - name: Run Python Unit Tests
              if: ${{ !cancelled() }}
              run: pytest -q

            # session-server/ is a separate package with its own
            # package.json, lockfile and tsconfig, and nothing checked it on a
            # PR: the root lint/test jobs never touch it, and the only thing
            # that compiled it — Build Sessions Image — is skipped on pull
            # requests. So a change that broke it (its own dependency bumps
            # very much included) stayed invisible until a push to master or a
            # tag. Type-check it here, where it's cheap.
            # `npm ci`, so lockfile drift fails the check loudly instead of
            # being silently reconciled the way `npm install` does — that's how
            # the lock went stale unnoticed in the first place (#345).
            - name: Install session-server dependencies
              if: ${{ !cancelled() }}
              working-directory: session-server
              run: npm ci
              env:
                  NPM_TOKEN: ${{ secrets.CLASSIC_ACCESS_TOKEN || secrets.HIVE_REPO_TOKEN }}

            - name: Lint session-server
              if: ${{ !cancelled() }}
              working-directory: session-server
              run: npm run lint

            - name: Build session-server
              if: ${{ !cancelled() }}
              working-directory: session-server
              run: npm run build

    build-ui:
        name: Build UI Image
        needs: [verify]
        if: github.ref == 'refs/heads/master' || github.ref == 'refs/heads/dev' || startsWith(github.ref, 'refs/tags/v')
        # GitHub-hosted runners ship Docker + buildx out of the box; this job
        # only builds/pushes an image, it doesn't need the self-hosted box.
        runs-on: ubuntu-latest
        timeout-minutes: 45
        permissions:
            contents: read
            packages: write

        steps:
            - name: Checkout repository
              uses: actions/checkout@v7

            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@v4
              with:
                  name: builder-ui
                  cleanup: false

            - name: Log in to GitHub Container Registry
              if: startsWith(github.ref, 'refs/tags/v')
              uses: docker/login-action@v4
              with:
                  registry: ${{ env.REGISTRY }}
                  username: ${{ github.actor }}
                  password: ${{ secrets.GITHUB_TOKEN }}

            - name: Extract metadata for UI Image
              id: meta
              uses: docker/metadata-action@v6
              with:
                  images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME_UI }}
                  tags: |
                      type=raw,value=latest,enable={{is_default_branch}}
                      type=ref,event=tag
                      type=sha,format=short

            - name: Build and push UI Image
              uses: docker/build-push-action@v7
              with:
                  context: .
                  file: ui/Dockerfile
                  push: ${{ startsWith(github.ref, 'refs/tags/v') }}
                  tags: ${{ steps.meta.outputs.tags }}
                  labels: ${{ steps.meta.outputs.labels }}
                  build-args: |
                      WEBSOCKET_SESSION_SERVER_SENDER_AUTH_KEY=ci_build_placeholder
                      NPM_TOKEN=${{ secrets.CLASSIC_ACCESS_TOKEN || secrets.HIVE_REPO_TOKEN }}
                  cache-from: type=gha
                  cache-to: type=gha,mode=max

    build-sessions:
        name: Build Sessions Image
        needs: [verify]
        if: github.ref == 'refs/heads/master' || github.ref == 'refs/heads/dev' || startsWith(github.ref, 'refs/tags/v')
        runs-on: ubuntu-latest
        timeout-minutes: 45
        permissions:
            contents: read
            packages: write

        steps:
            - name: Checkout repository
              uses: actions/checkout@v7

            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@v4
              with:
                  name: builder-sessions
                  cleanup: false

            - name: Log in to GitHub Container Registry
              if: startsWith(github.ref, 'refs/tags/v')
              uses: docker/login-action@v4
              with:
                  registry: ${{ env.REGISTRY }}
                  username: ${{ github.actor }}
                  password: ${{ secrets.GITHUB_TOKEN }}

            - name: Extract metadata for Sessions Image
              id: meta
              uses: docker/metadata-action@v6
              with:
                  images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME_SESSIONS }}
                  tags: |
                      type=raw,value=latest,enable={{is_default_branch}}
                      type=ref,event=tag
                      type=sha,format=short

            - name: Build and push Sessions Image
              uses: docker/build-push-action@v7
              with:
                  context: session-server/
                  file: session-server/Dockerfile.websocket
                  push: ${{ startsWith(github.ref, 'refs/tags/v') }}
                  tags: ${{ steps.meta.outputs.tags }}
                  labels: ${{ steps.meta.outputs.labels }}
                  build-args: |
                      NPM_TOKEN=${{ secrets.CLASSIC_ACCESS_TOKEN || secrets.HIVE_REPO_TOKEN }}
                  cache-from: type=gha
                  cache-to: type=gha,mode=max

    build-proxy:
        name: Build Proxy Image
        # Same branch-only scoping as `verify` above: without it a release tag,
        # which points at the bump commit, skips its own proxy build.
        if: >-
            (github.ref == 'refs/heads/master' || github.ref == 'refs/heads/dev' || startsWith(github.ref, 'refs/tags/v'))
            && !(github.ref_type == 'branch'
                 && startsWith(github.event.head_commit.message, 'chore: bump version to'))
        runs-on: ubuntu-latest
        timeout-minutes: 45
        permissions:
            contents: read
            packages: write

        steps:
            - name: Checkout repository
              uses: actions/checkout@v7

            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@v4
              with:
                  name: builder-proxy
                  cleanup: false

            - name: Log in to GitHub Container Registry
              if: startsWith(github.ref, 'refs/tags/v')
              uses: docker/login-action@v4
              with:
                  registry: ${{ env.REGISTRY }}
                  username: ${{ github.actor }}
                  password: ${{ secrets.GITHUB_TOKEN }}

            - name: Extract metadata for Proxy Image
              id: meta
              uses: docker/metadata-action@v6
              with:
                  images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME_PROXY }}
                  tags: |
                      type=raw,value=latest,enable={{is_default_branch}}
                      type=ref,event=tag
                      type=sha,format=short

            - name: Build and push Proxy Image
              uses: docker/build-push-action@v7
              with:
                  context: nginx/
                  file: nginx/Dockerfile.proxy
                  push: ${{ startsWith(github.ref, 'refs/tags/v') }}
                  tags: ${{ steps.meta.outputs.tags }}
                  labels: ${{ steps.meta.outputs.labels }}
                  cache-from: type=gha
                  cache-to: type=gha,mode=max

    craft-release:
        name: Craft Release Packages
        needs: [build-ui, build-sessions, build-proxy]
        if: startsWith(github.ref, 'refs/tags/v')
        runs-on: ubuntu-latest
        timeout-minutes: 45
        permissions:
            contents: write
            packages: read

        steps:
            - name: Checkout repository
              uses: actions/checkout@v7

            - name: Log in to GitHub Container Registry
              uses: docker/login-action@v4
              with:
                  registry: ${{ env.REGISTRY }}
                  username: ${{ github.actor }}
                  password: ${{ secrets.GITHUB_TOKEN }}

            - name: Setup Python
              uses: actions/setup-python@v7
              with:
                  python-version: "3.11"

            # The offline bundle vendors the CLI wheel (app.json
            # bundle.local_wheels) and installs it into the bundle's .venv.
            - name: Build Python CLI
              run: |
                  python -m pip install --upgrade pip build
                  python -m build cli/

            # Everything the old Prepare Workspace / Export Offline Images /
            # Vendor wheels / Compress steps did — and the guards they grew for
            # #451, #452 and #479 — now lives in sb90-deploy, driven by
            # deploy/app.json. Images come from docker-compose.release.yml, so
            # the offline tars and an online pull cannot disagree.
            - name: Craft release bundles
              uses: System-B90/.github/actions/craft-release@main
              with:
                  tag: ${{ github.ref_name }}
                  out: .

            - name: Publish GitHub Release
              uses: softprops/action-gh-release@v3
              with:
                  files: |
                      bluz-online-${{ github.ref_name }}.tar.gz
                      bluz-offline-${{ github.ref_name }}.tar.gz
                      cli/dist/*
                  generate_release_notes: true
                  draft: false
                  prerelease: false

    publish-cli-index:
        name: Publish bluz CLI to org pip index
        needs: craft-release
        if: startsWith(github.ref, 'refs/tags/v')
        runs-on: ubuntu-latest
        timeout-minutes: 45

        steps:
            - name: Checkout repository
              uses: actions/checkout@v7

            - name: Setup Python
              uses: actions/setup-python@v7
              with:
                  python-version: "3.11"

            - name: Build Python CLI
              run: |
                  python -m pip install --upgrade pip build
                  python -m build cli/

            # bluz is private; the .github repo is public and needs no Bluz
            # source or auth to install from — mirrors pyhive's publish.yml.
            - name: Checkout .github index repo
              uses: actions/checkout@v7
              with:
                  repository: System-B90/.github
                  token: ${{ secrets.CLASSIC_ACCESS_TOKEN }}
                  path: org-github

            - name: Copy distributions and regenerate index
              run: |
                  mkdir -p org-github/pypi/bluz-cli
                  cp cli/dist/* org-github/pypi/bluz-cli/
                  python org-github/pypi/generate_index.py bluz-cli

            - name: Commit and push index update
              working-directory: org-github
              run: |
                  git config user.name "github-actions[bot]"
                  git config user.email "github-actions[bot]@users.noreply.github.com"
                  git add pypi/
                  git diff --cached --quiet && exit 0
                  git commit -m "Vibe-Updated pip index for bluz-cli ${{ github.ref_name }}"
                  git push
